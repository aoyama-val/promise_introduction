//=============================================================================
//   Promiseオブジェクトの作り方
//=============================================================================

// new Promise()をするとPromiseオブジェクトを作成できます。
// new Promise()の引数には、2引数の関数を与えます。
// ここでpはPromiseオブジェクトです。
var p = new Promise(function(resolve, reject) {
});

console.log(p);
// => Promise { <pending> }
